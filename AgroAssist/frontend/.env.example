# Frontend Configuration

# API Configuration
# The backend API URL is configured in vite.config.js as a proxy
# Default: http://localhost:5000
# 
# To change the backend URL, edit the proxy configuration in vite.config.js

# Development Server Port
# Default: 3000 (configured in vite.config.js)
