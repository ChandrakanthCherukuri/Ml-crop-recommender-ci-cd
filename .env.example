# Environment variables for AgroAssist
# Copy this file to AgroAssist/backend/.env and fill in the values

# Backend Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration
# For local MongoDB:
# MONGO_URI=mongodb://localhost:27017/agroassist
# For MongoDB Atlas (recommended):
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database-name?retryWrites=true&w=majority

# JWT Secret (generate a secure random string)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
JWT_SECRET=your-secure-jwt-secret-key-change-this

# Frontend URL
FRONTEND_URL=http://localhost:5173

# Docker Configuration (for production)
DOCKER_REGISTRY=docker.io
DOCKER_USERNAME=your-dockerhub-username
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=change-this-secure-password
